# SHT3x 48-Hour Intensive Soak Attempt - COM20 - 2026-06-26

## Verdict

The 48-hour intensive soak did not start. The requested SHT3x HIL port,
`COM20`, is still inaccessible from this shell because Windows returns
`PermissionError(13, Access is denied)`.

No SHT3x CLI was reachable on any accessible serial port, so no soak result is
claimed.

## Latest Port Probe

| Port | Result |
| --- | --- |
| `COM5` | Cannot open, access denied |
| `COM19` | Opens, responds as `TunnelMonitor-node`; not SHT3x |
| `COM20` | Cannot open, access denied |
| `COM21` | Opens, responds as INA228 firmware; not SHT3x |
| `COM27` | Cannot open, currently held by another HIL runner process |
| `COM28` | Opens, responds as BME280 firmware; not SHT3x |

Observed `COM20` error:

```text
SerialException("could not open port 'COM20': PermissionError(13, 'Access is denied.', None, 5)")
```

An existing process was observed using `COM27`:

```text
python.exe tools\hil_cli_runner.py --port COM27 --baud 115200 ...
```

That process was not started by this turn and was left untouched.

## Soak Status

- Requested duration: 48 hours
- Started: no
- Completed: no
- Elapsed soak time: 0 seconds
- Hardware samples collected: 0
- Pass/fail/error counts: not available
- Reason: no reachable SHT3x HIL control port

## Command To Run Once COM20 Is Available

The current HIL runner supports count-based soak, not a native 48-hour duration
argument. After `COM20` is released and confirmed to run the SHT3x CLI, run an
initial calibration pass to measure cycle time, then calculate the required
`--soak-count` for 48 hours.

```powershell
pio run -e esp32s3dev -t upload --upload-port COM20
python tools/run_i2c_hil.py --port COM20 --baud 115200 --expect-address 0x44 --board esp32s3dev --target-name SHT3x-COM20 --include-clock-stretch --include-alert-write --include-all-periodic-rates --include-soak --soak-count <COUNT_FOR_48_HOURS>
```

Do not include destructive, fault-injection, or bus-wide reset options in a long
soak unless the fixture is explicitly built for those tests.
