# SHT3x v1.4.1 Release Notes
Date: 2026-04-03

## Highlights
- Aligned README installation, documentation, and repository references with the current GitHub source of truth.
- Standardized the example `Wire` transport around the shared `i2cUser` / `TwoWire*` callback contract used across the workspace.
- Updated the bringup example and native tests so the documented transport contract matches the exercised one.
- Standardized generated version metadata so the released `Version.h` follows the same macro-based pattern as the other libraries.

## Compatibility
- No library API changes in this release.
- This is a documentation, examples, and test-consistency patch release.

## Tag
- `v1.4.1`

## Suggested GitHub Release Title
- `SHT3x v1.4.1`
